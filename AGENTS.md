# AGENTS.md

## プロジェクトの位置づけ

このリポジトリは、OpenRTM 1.1 環境で humanoid robot を制御するためのコードを扱う。

この `AGENTS.md` は、プロジェクト全体で常に守るべき開発方針を定める。

---

## 仕様策定時の方針

私の意見や提案を否定することをためらわないこと。
私が方針に対して私見を述べることが多々あるが、それらはすべて1つの参考意見程度に留め、客観的根拠に基づいた思考を行ったうえで方針を決定すること。
私の要望や実装方針に関する不明点があれば、推測して進めるのではなく積極的に質問して仕様の詳細な策定を行っていくこと。

---

## 実装の基本方針

C++ node として開発する。

単一の巨大な callback に全処理を詰め込まないこと。
少なくとも以下の責務は分離すること。

状態遷移は明示的に扱う。
隠れた global state や、callback 間で追跡しにくい副作用を避ける。

---

## C++ 実装方針

C++ で実装する。

推奨する基本方針:

* 小さな class に責務を分離する
* 生 pointer より RAII と値所有を優先する
* 必要な場合のみ smart pointer を使う
* 例外を多用せず、明示的な状態・戻り値を使う
* magic number を避け、parameter または名前付き定数にする

---

## clang-format

本リポジトリでは `clang-format` を適用しないこと。
既存のコードスタイルに合わせた編集を行うこと。

---

## 安全方針

実ロボットに指令を送るコードを扱っている。
挙動が曖昧な場合は、安全側の挙動を選ぶこと。

特に以下を守る。

* 指令値を不連続に急変させない
* 意図しない残留動作を避ける
* 内部で無制限に蓄積される hidden goal を作らない
* mode 切替時の速度指令は limiter / filter を通す

---

## 座標規約

明示的に別指定がない限り、choreonoidの座標規約に従う。

---

## ビルド

IDLファイルに変更を行った後の初回ビルドは`catkin build auto_stabilizer --no-deps --force-cmake`。
それ以外の場合では`catkin build auto_stabilizer --no-deps`。

---

## 仕様文書の扱い

詳細な挙動、parameter、mapping、制御則、テスト項目、acceptance criteria は、タスクごとの計画書に記述する。

`AGENTS.md` には、今回だけの詳細仕様を重複して書かない。
`AGENTS.md` は安定したプロジェクト方針に留める。

もし `AGENTS.md` と、より新しい明示的な計画書・タスク指示が矛盾しているように見える場合は、推測で実装せず確認すること。

---

## テスト方針

本リポジトリではテストの作成は不要である。

---

## コードスタイル

読みやすく保守しやすい C++ を書く。
ただし本リポジトリはforkであり、既存のコードのフォーマットを崩さないようにすること。

* 名前は具体的にする
* 責務の境界を明確にする
* robot-specific assumption は局所化する
* 座標系、安全上の判断、状態遷移などの非自明な箇所はコメントで説明する
* 不要に広範囲な rewrite をしない

class 名、関数名、変数名は原則として英語を使う。

また、コメントやmarkdownファイルは日本語で記述すること。
